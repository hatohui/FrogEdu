export enum CognitiveLevel {
	Easy = 0,
	Medium = 1,
	Hard = 2,
}

export enum QuestionType {
	MultipleChoice = 0,
	TrueFalse = 1,
	Essay = 2,
	FillInBlank = 3,
}

export enum QuestionSource {
	Manual = 0,
	AIGenerated = 1,
}
